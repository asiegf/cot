# Claude Instructions

Follow `AGENTS.md` as the canonical agent entry point for this repository.

Before starting work, read `ai/AGENT_RULES.md`. This project uses **bd**
(Beads) for issue tracking, so run `bd prime` for workflow context and use
`bd` commands for task state.

Keep the non-interactive shell guidance in `AGENTS.md` in mind when running
file operations or commands that may prompt.
